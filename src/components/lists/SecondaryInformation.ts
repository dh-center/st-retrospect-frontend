import styled from 'styled-components';
import { sansSerifLight } from '../../styles/FontStyles';

/**
 * Text with secondary information in items
 */
export default styled.span`
  ${ sansSerifLight };
  font-size: 14px;
`;
