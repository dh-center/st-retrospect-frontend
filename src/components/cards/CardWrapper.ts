import styled from 'styled-components';

/**
 * Wrapper for card block
 */
export default styled.div`
  display: flex;
  flex-direction: column;

  position: relative;

  margin: 0 -16px;
  overflow-y: hidden;
`;
