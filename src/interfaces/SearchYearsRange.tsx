/**
 * Interface of range (min and max) years for passing to SearchForm
 */
export interface SearchYearsRange {
  /**
   * Min value of years
   */
  min: string;

  /**
   * Max value of years
   */
  max: string;
}
