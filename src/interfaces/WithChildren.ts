import { ReactNode } from 'react';

/**
 * Default interface with children components in props
 */
interface WithChildren {
  /**
   * Children elements
   */
  children?: ReactNode;
}

export default WithChildren;
